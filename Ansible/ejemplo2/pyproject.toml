[project]
name = "ejemplos2"
version = "0.1.0"
description = "ejemplos de playbooks con estructuras de programacion de ansible"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=13.0.0",
    "jsonschema>=4.25.1",
]
