[project]
name = "ejemplo3"
version = "0.1.0"
description = "Ansible Vault example project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=13.1.0",
    "ansible-pylibssh>=1.3.0",
]
