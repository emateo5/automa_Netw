[project]
name = "ejemplo1"
version = "0.1.0"
description = "Primer proyecto Ansible para automatizacion de redes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=12.2.0",
    "ansible-pylibssh>=1.3.0",
    "paramiko>=4.0.0",
]
